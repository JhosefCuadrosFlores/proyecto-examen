services:
  mysql:
    image: mysql:8.0
    container_name: comedor-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-comedor_root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-restauranteupeuu}
      MYSQL_USER: ${MYSQL_USER:-comedor}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-comedor123}
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-comedor_root}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  mysql_data:
